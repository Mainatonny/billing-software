{
  "name": "frontend",
  "version": "1.0.0",
  "homepage": "https://Mainatonny.github.io/billing-software",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },

  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
